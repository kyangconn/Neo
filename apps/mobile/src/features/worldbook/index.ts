/**
 * @mobile/features/worldbook
 *
 * 世界书（Lorebook）管理。
 *
 * 模块规划:
 * - `WorldbookScreen.tsx` 世界书条目列表。
 * - `WorldbookEntryEditor.tsx` 条目编辑（keyword + entry text）。
 * - `worldbook.store.ts` 世界书状态。
 *
 * 约定:
 * - 召回逻辑（关键词匹配）从 packages/core 复用，不在此重复实现。
 * - 条目数据结构对齐 @neo-tavern/shared 的 WorldbookEntry。
 */

// 占位 — 实现待补。

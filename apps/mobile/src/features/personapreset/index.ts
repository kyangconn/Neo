/**
 * @mobile/features/personapreset
 *
 * 用户人设 + 预设提示词。
 *
 * 设计理由：人设和预设都是"影响对话上下文的小型文本配置"，
 * 放在同一个 domain group 减少过度拆分。
 *
 * 模块规划:
 * - `persona.screen.tsx` 用户人设编辑（name + desc）。
 * - `preset.screen.tsx`  预设列表 / 编辑。
 * - `personapreset.store.ts` 人设 + 预设状态（persist 到 KV）。
 */

// 占位 — 实现待补。

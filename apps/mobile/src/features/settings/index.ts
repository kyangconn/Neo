/**
 * @mobile/features/settings
 *
 * 设置页。
 *
 * 模块规划:
 * - `SettingsScreen.tsx` 设置主页面（分组）。
 * - `ModelConfigScreen.tsx` API / 模型配置。
 * - `ThemeSection.tsx` 主题切换（system / light / dark）。
 * - `settings.store.ts` zustand store（偏好 + 模型配置）。
 *
 * 约定:
 * - 设置持久化走 settings.store + zustand persist。
 * - API key 不落盘（仅内存），在 persist partialize 中排除。
 */

// 占位 — 实现待补。

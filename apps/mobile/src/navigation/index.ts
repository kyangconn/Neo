/**
 * @mobile/navigation
 *
 * React Navigation 配置。
 *
 * 模块规划:
 * - `RootNavigator.tsx` 顶部导航容器（Tab + Stack）。
 * - `types.ts` 路由参数类型。
 *
 * 约定:
 * - 所有 screen 注册在此，不在 feature 里自建导航。
 * - 路由参数使用 TypeScript 类型约束。
 */

// 占位 — 实现待补。

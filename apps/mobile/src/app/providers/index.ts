/**
 * @mobile/app/providers
 *
 * React context providers（SafeArea / Store / Theme 等）。
 *
 * 约定:
 * - 每个 provider 是独立文件，通过 index 统一导出。
 * - Provider 不包含业务逻辑（业务逻辑在 service / store 中）。
 */

// 占位 — 实现待补。

/**
 * @mobile/hooks
 *
 * 跨 feature 共享的 React hooks。
 *
 * 举例:
 * - useAppState（前后台切换）。
 * - useDebounce。
 * - useNetworkStatus（NetInfo 封装）。
 *
 * 约定:
 * - 不在此放 zustand store（那些放 src/store）。
 * - 不在此放 feature 专用 hook（那些放 feature/<name>/）。
 */

// 占位 — 实现待补。

/**
 * @mobile/components
 *
 * 跨 feature 共享的 UI 组件。
 *
 * 举例:
 * - Button / Card / Input / Modal。
 * - 列表骨架屏 / 空状态 / 错误状态。
 *
 * 约定:
 * - 不在此放 feature 专用组件（那些放 feature/<name>/）。
 * - 不在此写业务逻辑（只渲 UI，数据通过 props 传入）。
 */

// 占位 — 实现待补。
